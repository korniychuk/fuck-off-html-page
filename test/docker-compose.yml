version: '3'

services:
  shell2http:
    build: .
    ports:
      - 8000:8080
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "$(realpath $(where docker)):/usr/bin/docker"
      - .

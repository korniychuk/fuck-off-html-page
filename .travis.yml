language: ruby

git:
  depth: 1

services:
  - docker

script:
  - docker build -t $DOCKER_PRIVATE_REGISTRY/fuck-off-html-page .

deploy:
  provider: script
  script: ./docker-push.sh
  on:
    branch: master

after_deploy:
  - ./trigger-pull.sh
